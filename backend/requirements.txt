# Core
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# AI — OpenAI Responses API is called via raw httpx (see app/services/llm.py); no SDK needed.

# AWS (Secrets Manager loader — app/services/secrets.py)
boto3>=1.34.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
# Postgres driver for production (local sqlite needs neither)
psycopg[binary]>=3.1.0

# Validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.26.0

# Migrations
alembic>=1.13.0

# Rate limiting
slowapi>=0.1.9

# Multipart form parsing (speaking audio uploads)
python-multipart>=0.0.26

# Dev tools
ruff>=0.2.0
